package com.matheus.crud.back_video_3.dtos;

// Classe DTO (Data Transfer Object) para representar mensagens de erro
public record ErrorDto(String message) {
    // Apenas guarda a mensagem de erro
}
